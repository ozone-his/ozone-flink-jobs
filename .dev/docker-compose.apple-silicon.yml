# Apple Silicon override. Only debezium/connect lacks an arm64 image now, so it is the only service
# forced to amd64 emulation. Kafka (apache/kafka), Redpanda Console and the databases are all
# multi-arch and run natively.
services:
  connect:
    platform: linux/amd64
